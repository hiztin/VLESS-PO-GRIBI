# 2. Триггер при изменении кода
name: 🔧 Обновление при изменении кода

on:
  push:
    paths:
      - 'source/parser.py'
      - 'source/requirements.txt'
      - '.github/workflows/**'

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 🚀 Запуск парсера
        working-directory: ./source
        run: python parser.py
      - name: 📤 Коммит
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add deploy/
          git diff --staged --quiet || git commit -m "🔄 Обновление после изменения кода"
          git push
