name: 🔄 Автообновление конфигов

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.MY_PAT }}
          fetch-depth: 0
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14'
          
      - run: pip install aiohttp
      
      - working-directory: ./source
        run: python parser.py
      
      - name: ПРИНУДИТЕЛЬНЫЙ КОММИТ
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # Добавляем всё
          git add -f deploy/
          git add -f README.md
          
          # Создаём пустой файл с timestamp, если нет других изменений
          if git diff --staged --quiet; then
            echo "🕒 Принудительное обновление $(date)" > .force-update
            git add .force-update
          fi
          
          # Всегда делаем коммит
          git commit -m "🔄 Обновление $(date +'%Y-%m-%d %H:%M:%S')"
          git push
