name: 🔄 Автообновление конфигов

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: 🐍 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: 📦 Зависимости
        run: pip install aiohttp
          
      - name: 🚀 Парсер
        working-directory: ./source
        run: python parser.py
        
      - name: 🔍 Проверка
        run: |
          echo "📁 Файлы в deploy:"
          ls -la deploy/
          echo "📁 Файлы в subscriptions:"
          ls -la deploy/subscriptions/ || echo "Нет файлов"
          
      - name: 📤 Коммит и пуш
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # Добавляем всё из deploy
          git add deploy/
          
          # Проверяем изменения
          if git diff --staged --quiet; then
            echo "ℹ️ Нет изменений"
          else
            git commit -m "🔄 Обновление $(date +'%Y-%m-%d %H:%M UTC')"
            git push
            echo "✅ Изменения отправлены"
            
            # Показываем, что добавилось
            echo "📄 Новые файлы:"
            git diff --staged --name-only
          fi
