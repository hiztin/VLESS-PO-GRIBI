name: 🔄 Ежедневное обновление подписок

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:
  push:
    paths:
      - 'parser.py'
      - 'requirements.txt'
      - '.github/workflows/*'

jobs:
  update-subscriptions:
    name: Сбор и проверка серверов
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Явно даем права на запись
      actions: read
    
    steps:
      - name: 📥 Клонирование репозитория
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      
      
      - name: 🚀 Запуск парсера
        run: python parser.py
        continue-on-error: false  # Лучше false, чтобы видеть реальные ошибки
      
      - name: 🔍 Проверка созданных файлов
        run: |
          echo "📁 Содержимое deploy после парсера:"
          find deploy -type f -name "*.txt" | sort || echo "Нет .txt файлов"
          echo "📊 Статистика файлов:"
          wc -l deploy/*.txt 2>/dev/null || echo "Нет текстовых файлов"
      
      - name: 📦 Коммит и пуш изменений
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # Добавляем все изменения в deploy
          git add deploy/
          
          # Проверяем, есть ли изменения для коммита
          if git diff --cached --quiet; then
            echo "ℹ️ Нет изменений для коммита"
          else
            git commit -m "🔄 Обновление подписок $(date +'%Y-%m-%d %H:%M UTC')"
            git push
            echo "✅ Изменения отправлены"
          fi
      
      - name: 📤 Загрузка артефактов (для резервного копирования)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: subscriptions-${{ github.run_id }}
          path: deploy/
          retention-days: 7
