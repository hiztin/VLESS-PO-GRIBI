name: 🔄 Ежедневное обновление подписок

on:
  schedule:
    - cron: '0 */3 * * *'   # Каждые 3 часа
  workflow_dispatch:         # Ручной запуск
  push:
    paths:
      - 'parser.py'
      - 'requirements.txt'
      - '.github/workflows/*'

jobs:
  update-subscriptions:
    name: Сбор и проверка серверов
    runs-on: ubuntu-latest
    permissions:
      contents: write      # Права на запись в репозиторий
    
    steps:
      - name: 📥 Клонирование репозитория
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 🐍 Установка Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: 📦 Установка зависимостей
        run: |
          pip install --upgrade pip
          pip install httpx beautifulsoup4 lxml aiofiles
      
      - name: 🚀 Запуск парсера
        id: parser
        run: python parser.py
        continue-on-error: true
      
      - name: 📊 Проверка результатов
        id: check
        run: |
          if [ -d "deploy" ] && [ "$(ls -A deploy)" ]; then
            echo "✅ Папка deploy существует"
            echo "files_exist=true" >> $GITHUB_OUTPUT
            
            # Статистика
            if [ -f "deploy/debug.json" ]; then
              ALIVE=$(jq -r '.alive' deploy/debug.json 2>/dev/null || echo "0")
              TOTAL=$(jq -r '.total' deploy/debug.json 2>/dev/null || echo "0")
              echo "📊 Серверов: $TOTAL, рабочих: $ALIVE"
            fi
            
            # Список файлов
            echo "📄 Созданные файлы:"
            find deploy -type f -name "*.txt" | sort
          else
            echo "⚠️ Папка deploy пуста"
            echo "files_exist=false" >> $GITHUB_OUTPUT
          fi
      
      - name: 📦 Коммит и пуш обновлений
        if: steps.check.outputs.files_exist == 'true'
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          git add deploy/
          
          if git diff --cached --quiet; then
            echo "ℹ️ Нет изменений для коммита"
          else
            git commit -m "🔄 Автообновление подписок $(date +'%Y-%m-%d %H:%M UTC')"
            git push
            echo "✅ Изменения отправлены в репозиторий"
          fi
      
      - name: 📤 Загрузка артефактов
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: subscriptions-${{ github.run_id }}
          path: deploy/
          retention-days: 7
          if-no-files-found: warn
